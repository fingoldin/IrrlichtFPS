#pragma once

#include "stdafx.h"

#include "Core.h"

class InputContext
{
public:
	InputContext() {}
	virtual ~InputContext()
	{
		
	}

	//virtual void update(Core * core) = 0;

	//virtual bool handleEvent(Core * core, const irr::SEvent& event) = 0;

};
